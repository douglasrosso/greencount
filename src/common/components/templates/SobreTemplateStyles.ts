import styled from "styled-components";

export const Sobre = styled.main`
  width: 80%;
  margin-top: 1%;
  display: flex;
  
  article {
    text-align: center;
    position: relative;
    display: grid;

    img {
      height: 350px;
      border-radius: 10px;
    }
    span {
      font-weight: bold;
      /* color: rgba(0, 212, 255, 0.4); */
    }
  }
`;
